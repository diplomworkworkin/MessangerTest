<Window x:Class="ClinicApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ClinicApp.Wpf.ViewModels"
        xmlns:views="clr-namespace:ClinicApp.Wpf.Views"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PatientsViewModel}">
            <views:PatientsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DoctorsViewModel}">
            <views:DoctorsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ScheduleViewModel}">
            <views:ScheduleView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar (Visible only when logged in) -->
        <Border Grid.Column="0" Background="#F0F0F0" BorderBrush="LightGray" BorderThickness="0,0,1,0"
                Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="10,20">
                    <TextBlock Text="ClinicApp" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentUserFullName}" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0" Foreground="Gray"/>
                </StackPanel>
                
                <StackPanel Grid.Row="1" Margin="10">
                    <Button Content="Главная" 
                            Command="{Binding NavigateToDashboardCommand}" 
                            Margin="0,5" Padding="10"/>
                    
                    <Button Content="Пациенты" 
                            Command="{Binding NavigateToPatientsCommand}" 
                            Visibility="{Binding IsRegistrar, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="0,5" Padding="10"/>
                    
                    <Button Content="Врачи" 
                            Command="{Binding NavigateToDoctorsCommand}" 
                            Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="0,5" Padding="10"/>
                    
                    <Button Content="Расписание" 
                            Command="{Binding NavigateToScheduleCommand}" 
                            Visibility="{Binding IsRegistrar, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="0,5" Padding="10"/>
                    
                    <Button Content="Настройки" 
                            Command="{Binding NavigateToSettingsCommand}" 
                            Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="0,5" Padding="10"/>
                </StackPanel>

                <Button Grid.Row="2" Content="Выход" 
                        Command="{Binding LogoutCommand}" 
                        Margin="10" Padding="10" Background="#FFE0E0"/>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <ContentControl Grid.Column="1" Content="{Binding Navigation.CurrentView}">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Style.Triggers>
                        <EventTrigger RoutedEvent="Binding.TargetUpdated">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.3"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
        
        <!-- Full screen Login (when not logged in) -->
        <ContentControl Grid.ColumnSpan="2" Content="{Binding Navigation.CurrentView}"
                        Visibility="{Binding IsLoggedIn, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
    </Grid>
</Window>
