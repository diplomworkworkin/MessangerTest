<Window x:Class="ClinicApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClinicApp.Wpf"
        xmlns:vm="clr-namespace:ClinicApp.Wpf.ViewModels"
        xmlns:views="clr-namespace:ClinicApp.Wpf.Views"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="600" Width="1000">
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PatientsViewModel}">
            <views:PatientsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DoctorsViewModel}">
            <views:DoctorsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ScheduleViewModel}">
            <views:ScheduleView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="#F3F3F3" BorderBrush="#DDDDDD" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <TextBlock Text="ClinicApp" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                
                <Button Content="Главная" 
                        Command="{Binding NavigateToDashboardCommand}" 
                        Margin="0,5" Padding="10"/>
                <Button Content="Пациенты" 
                        Command="{Binding NavigateToPatientsCommand}" 
                        Margin="0,5" Padding="10"/>
                <Button Content="Врачи" 
                        Command="{Binding NavigateToDoctorsCommand}" 
                        Margin="0,5" Padding="10"/>
                <Button Content="Расписание" 
                        Command="{Binding NavigateToScheduleCommand}" 
                        Margin="0,5" Padding="10"/>
                <Button Content="Настройки" 
                        Command="{Binding NavigateToSettingsCommand}" 
                        Margin="0,5" Padding="10"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <ContentControl Content="{Binding Navigation.CurrentView}">
                <ContentControl.Resources>
                    <Style TargetType="ContentPresenter">
                        <Style.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1" Duration="0:0:0.3"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>
    </Grid>
</Window>
