<UserControl x:Class="ClinicApp.Wpf.Views.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Расписание приёмов" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Add Appointment Form -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Новая запись" FontWeight="Bold" Margin="0,0,0,10"/>
                <UniformGrid Columns="3">
                    <StackPanel Margin="5">
                        <TextBlock Text="Пациент"/>
                        <ComboBox ItemsSource="{Binding Patients}" SelectedItem="{Binding SelectedPatient}" DisplayMemberPath="FullName"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Врач"/>
                        <ComboBox ItemsSource="{Binding Doctors}" SelectedItem="{Binding SelectedDoctor}" DisplayMemberPath="FullName"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Дата"/>
                        <DatePicker SelectedDate="{Binding AppointmentDate}"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Время (ЧЧ:ММ)"/>
                        <TextBox Text="{Binding StartTimeStr, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Длительность (мин)"/>
                        <TextBox Text="{Binding DurationMinutes, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Margin="5">
                        <TextBlock Text="Заметки"/>
                        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </UniformGrid>
                
                <Grid Margin="5,10,5,0">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" VerticalAlignment="Center"/>
                    <Button Content="Записать" Command="{Binding AddAppointmentCommand}" 
                            HorizontalAlignment="Right" Padding="20,5"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Appointments List -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Appointments}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Пациент" Binding="{Binding Patient.FullName}" Width="*"/>
                <DataGridTextColumn Header="Врач" Binding="{Binding Doctor.FullName}" Width="*"/>
                <DataGridTextColumn Header="Начало" Binding="{Binding StartTime, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Конец" Binding="{Binding EndTime, StringFormat='HH:mm'}" Width="80"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding IsCancelled, Converter={StaticResource BooleanToStatusConverter}}" Width="100"/>
                <DataGridTemplateColumn Header="Действия" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Отменить" 
                                    Command="{Binding DataContext.CancelAppointmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    IsEnabled="{Binding IsCancelled, Converter={StaticResource InverseBooleanConverter}}"
                                    Padding="5,2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="Загрузка..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
        </Grid>
    </Grid>
</UserControl>
