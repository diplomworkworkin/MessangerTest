<UserControl x:Class="ClinicApp.Wpf.Views.DoctorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление врачами" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Search and Add Form -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                         VerticalContentAlignment="Center" Padding="5" Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="Поиск" Command="{Binding SearchCommand}" Padding="20,5"/>
            </Grid>

            <Border BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Добавить нового врача" FontWeight="Bold" Margin="0,0,0,10"/>
                    <UniformGrid Columns="4">
                        <StackPanel Margin="5">
                            <TextBlock Text="Фамилия"/>
                            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="Имя"/>
                            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="Специальность"/>
                            <ComboBox ItemsSource="{Binding Specialties}" SelectedItem="{Binding Specialty}"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="Телефон"/>
                            <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </UniformGrid>
                    <Button Content="Добавить" Command="{Binding AddDoctorCommand}" 
                            HorizontalAlignment="Right" Margin="5" Padding="20,5"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Doctors List -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <DataGrid ItemsSource="{Binding Doctors}" 
                      SelectedItem="{Binding SelectedDoctor}"
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*"/>
                    <DataGridTextColumn Header="Специальность" Binding="{Binding Specialty}" Width="*"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button Grid.Row="1" Content="Удалить выбранного" 
                    Command="{Binding DeleteDoctorCommand}" 
                    HorizontalAlignment="Left" Margin="0,10,0,0" Padding="10,5" Background="#FFE0E0"/>
        </Grid>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="Загрузка..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
        </Grid>
    </Grid>
</UserControl>
