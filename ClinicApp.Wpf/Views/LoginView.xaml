<UserControl x:Class="ClinicApp.Wpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lottie="clr-namespace:LottieSharp.WPF;assembly=LottieSharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <materialDesign:Card Width="380" Padding="32" VerticalAlignment="Center" HorizontalAlignment="Center" UniformCornerRadius="12">
            <StackPanel>
                <lottie:LottieAnimationView FileName="Assets/loading.json" 
                                          AutoPlay="True" 
                                          RepeatCount="-1"
                                          Width="120" Height="120" 
                                          HorizontalAlignment="Center" 
                                          Margin="0,0,0,16"/>
                
                <TextBlock Text="ClinicApp" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="Вход в систему" FontSize="16" Opacity="0.6" HorizontalAlignment="Center" Margin="0,0,0,32"/>

                <TextBox materialDesign:HintAssist.Hint="Логин" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,0,16"/>

                <PasswordBox x:Name="PasswordBox" 
                             materialDesign:HintAssist.Hint="Пароль" 
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             Margin="0,0,0,24"/>

                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="{DynamicResource MaterialDesignValidationErrorBrush}" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,16"
                           TextWrapping="Wrap"/>

                <Button Content="ВОЙТИ" 
                        Command="{Binding LoginCommand}" 
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Height="48"
                        materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding IsBusy}"
                        materialDesign:ButtonProgressAssist.Value="-1"
                        materialDesign:ButtonProgressAssist.IsIndeterminate="True"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
