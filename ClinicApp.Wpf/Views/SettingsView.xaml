<UserControl x:Class="ClinicApp.Wpf.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Настройки" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Text="{Binding Message}" FontSize="16" Margin="0,0,0,20"/>
            
            <Border BorderBrush="Gray" BorderThickness="1" Padding="20" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Проверка базы данных" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding DbStatus}" Margin="0,0,0,10"/>
                    <Button Content="Проверить БД (Ping)" Command="{Binding CheckDbConnectionCommand}" Padding="10,5"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
